{% extends 'base.html.twig' %}

{% block title %}Ajouter un post{% endblock %}

{% block body %}
    {% if edit %}
    <h1>Editer un post</h1>
    {% else %}
    <h1>Ajouter un post</h1>
    {% endif %}
<div class="add">
    {{ form_start(formAddPost)}}
    {{ form_row(formAddPost)}}
    {{ form_end(formAddPost)}}
</div>
{% endblock %}